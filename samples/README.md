# Format examples only

These files demonstrate CSV structure and header encoding.

They contain synthetic dates and synthetic times. They are **not official
prayer-time tables** and must never be activated for real scheduling.

Use the relevant authority's current publication and review
[the table-format guide](../docs/table-format-guide.md).
